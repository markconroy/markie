{#
  I am removing everything from this, except the rendering of the actual field
  item(s), so we do not have any `.field__item` etc classes in our way.
#}

{% for item in items %}
  {{ item.content }}
{% endfor %}
