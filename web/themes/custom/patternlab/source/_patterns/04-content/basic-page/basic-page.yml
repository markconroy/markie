attributes:
  Attribute():
    class:

node:
  bundle: page
  isPublished: true

view_mode: full

label: <span class="field--name-title">Drupal Camp Dublin is Next Week - Last Chance for Tickets</span>

intro: <div class="field--name-field-intro"><p>Seems like just yesterday since we held DrupalCon in Dublin, now we're back with our annual Drupal Camp Dublin.</p></div>

building_blocks:
  - join():
    - include():
        pattern: building-blocks-text-paragraph-x1
  - join():
    - include():
        pattern: building-blocks-section-break
  - join():
    - include():
        pattern: building-blocks-text-paragraph-x2
